from backend.models.chat import ChatRequest, ChatResponse

def chat_reply(req: ChatRequest) -> ChatResponse:
    # Placeholder for Phase 2
    return ChatResponse(response="This is a static response placeholder.")
